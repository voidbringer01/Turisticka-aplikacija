import { VaznostfilterPipe } from './vaznostfilter.pipe';

describe('VaznostfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VaznostfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
